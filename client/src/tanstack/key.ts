import { uuid } from "../utils"

const queryKeyAllUser = `all-user-${uuid()}`
const queryKeyAllReversation = `all-reversation-${uuid()}`
const queryKeyAllRoom = `all-room-${uuid()}`
const queryKeyDataChartStatistic = `data-chart-statistic-${uuid()}`

export {
    queryKeyAllUser,
    queryKeyAllReversation,
    queryKeyAllRoom,
    queryKeyDataChartStatistic
}