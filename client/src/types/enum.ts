export enum MATH_ACTION {
    PLUS,
    MINUS
}

export enum POSITION {
    ADMIN = 1,
    HOUSEKEEPER = 2,
    RECEPTIONIST = 3,
    CUSTOMER = 4
}

export enum STATUS {
    WAITING = 1,
    OPEN = 2,
    IN_PROGRESS = 3,
    END = 4,
    CANCEL = 5
}